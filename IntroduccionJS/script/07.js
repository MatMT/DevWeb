// Orden de las operaciones
let resultado;
resultado = (20 + 30) * 2;
resultado = (600 + 600) * 1.05;

// console.log(resultado);

// Incrementos
let puntaje = 10;

// Incremento en 1 ++
puntaje++; // Primero muestra, luego incrementa (10)
++puntaje; // Incrementa y muestra (12)

// puntaje += 10; Incremento con otro valor

// Decremento en 1 --
puntaje--;
--puntaje;

console.log(puntaje);
