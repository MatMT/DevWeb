// Objeto Math

let resultado;

resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.2); // Ceil siempre redondea hacia arriba
resultado = Math.floor(2.9); // Floor siempre redondea hacia abajo
resultado = Math.sqrt(144); // Raíz cuadrada
resultado = Math.abs(-200); // Convierte a positivo
resultado = Math.min(3, 5, 1, 8, 2, 10); // Número menor de un listado
resultado = Math.max(3, 5, 1, 8, 2, 10); // Número mayor de un listado
resultado = Math.random(); // Número random entre 0 y 1
resultado = Math.floor(Math.random() * 30);

console.log(resultado);
